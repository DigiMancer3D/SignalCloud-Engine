# Point Cloud Paint++ starter templates

These descriptors document the nine +PCP+ environment modes and their suggested layer stacks. Branch 2 creates the same mode defaults through **File → New**. They remain external descriptors so future editor versions can add template import without changing or invalidating existing `.pcp3` projects.
