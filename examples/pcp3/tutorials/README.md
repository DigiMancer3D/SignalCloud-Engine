# PCP3 Branch 12 Tutorial Starters

These nine projects contain mode-template layers and documentation metadata, but no finished geometry. Copy a starter into `user_data/pcp3/projects/` before authoring. Runtime behavior is disabled by default.
